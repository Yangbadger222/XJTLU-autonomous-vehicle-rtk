# 2026年6月开发日志

---

## 2026.06.13

### [FRC 健康聚合节点 C++ 迁移]

#### File / 文件

- `src/frc/frc_nodes_cpp/package.xml`
- `src/frc/frc_nodes_cpp/CMakeLists.txt`
- `src/frc/frc_nodes_cpp/include/frc_nodes_cpp/health_aggregator_core.hpp`
- `src/frc/frc_nodes_cpp/include/frc_nodes_cpp/event_marker_core.hpp`
- `src/frc/frc_nodes_cpp/src/health_aggregator_node.cpp`
- `src/frc/frc_nodes_cpp/src/event_marker_node.cpp`
- `src/frc/frc_nodes_cpp/test/test_health_aggregator_core.py`
- `src/frc/frc_nodes_cpp/test/test_event_marker_core.py`
- `src/frc/frc_bringup/launch/frc_stack.launch.py`
- `src/frc/frc_bringup/package.xml`
- `Makefile`
- `docs-CN/knowledge/frc.md`
- `docs-EN/knowledge/frc.md`

#### Change / 改动

- 新增 `frc_nodes_cpp` C++ 包，提供 `frc_health_aggregator` 与 `frc_event_marker` 可执行文件。
- 将健康聚合的 stale 判断、LIO 退化判断、PGO 修正状态、RTK 状态和底盘速度聚合逻辑抽成无 ROS 依赖的 C++ 核心头文件。
- 将事件标记的接管边沿、SELECT 人工标记、stuck、near-collision、recovery、jerk、plan instability 规则迁到 C++，并保留 JSONL 镜像输出。
- `frc_stack.launch.py` 将 `frc_health_aggregator` 和 `frc_event_marker` 切到 C++ 包，其余 FRC 在线节点继续使用 Python `frc_nodes`。
- `make build-frc` 增加 `frc_nodes_cpp`，`frc_bringup` 增加运行依赖。
- 增加本机可运行的 `g++`/pytest 核心契约测试。

#### Reason / 原因

- `frc_health_aggregator` 与 `frc_event_marker` 都是长期常驻的 Python ROS 节点，且不负责模型推理/数据库写入，适合作为 FRC 在线节点 C++ 化的前两刀。
- 先迁低风险节点可以验证包结构、构建链和 launch 切换方式，同时保留 Python 实现作为回滚参照。

#### Effect / 影响

- FRC shadow/full 模式少两个 Python 常驻进程，预计可降低 Jetson 运行内存占用。
- `/frc/health` 与 `/frc/event_marker` 的话题、节点名和参数保持不变，下游 `frc_risk_pipeline` 与 `frc_memory_manager` 不需要改。
- 本机无 ROS Humble 环境，只完成无 ROS 核心测试、launch Python 语法、XML 解析、`git diff --check` 和 `make -n build-frc`；仍需在 Jetson 上执行 `colcon build --packages-select frc_msgs frc_nodes_cpp frc_bringup --symlink-install --parallel-workers 1`。

### [下位机急停倒转停止路径修复]

#### File / 文件

- `src/firmware/rm_c_board/Core/Src/main.c`
- `src/firmware/rm_c_board/Core/Src/Motor_Speed_pid.c`
- `src/firmware/rm_c_board/Core/Inc/Motor_Speed_pid.h`
- `src/firmware/rm_c_board/Core/Src/Joystick.c`
- `src/firmware/rm_c_board/Core/Src/pid.c`
- `src/firmware/rm_c_board/Makefile`
- `src/firmware/rm_c_board/test/test_estop_safety_contract.py`
- `src/firmware/rm_c_board/README.md`
- `docs-CN/hardware_spec.md`
- `docs-EN/hardware_spec.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/known_issues.md`
- `docs-EN/known_issues.md`

#### Change / 改动

- KEY 急停分支不再停跑后只闪灯，而是持续调用 `Emergency_Stop_Output()` 下发零电流 CAN 帧，并改为非阻塞红灯常亮。
- `Set_free;` / `led_white_start;` 无效表达式修复为函数调用，新增 `Emergency_Stop_Output()` 统一清零 `Vcx/Wc`、目标转速、PID 输出和目标。
- PS2 `B` 键从 PID 锁零速语义改为与 `X` 一致的撤力失能路径，并立即发送零电流帧。
- `pid.c` 恢复积分限幅、`last_err` 更新，并对 PID 时间步长做 1-50 ms 钳位；固件 Makefile 增加 `-Werror=unused-value`。
- 新增不依赖 ROS 的静态回归测试，锁定 KEY/B/X 停止路径与 PID 安全契约。
- 更新安全文档，明确 `X` 仍是最高优先级软件失能，红色物理急停覆盖所有软件命令，`B` 在台架/车测验证前不得替代急停。

#### Reason / 原因

- 旧 KEY 路径会设置 `stop_flag` 后停止任务调度，导致底盘 CAN 不再持续刷新，C620 可能锁存最后一帧电流。
- 旧 `Set_free;` 只是函数名表达式，不会真正调用零电流输出和 PID 清零逻辑。
- 旧 `B` 键通过 PID 锁零速主动刹车，配合未生效的积分限幅会造成过零后的反向电流输出。

#### Effect / 影响

- KEY、`X`、`B` 和手柄掉线停止路径都应进入零电流撤力滑停，而不是停发 CAN 或依赖 PID 抱死。
- 正常串口/手柄行驶路径的 PID 增益值未调整，但积分限幅现在生效；如台架发现跟踪变化，应单独评估 `IntegralLimit`，不要回滚停止路径安全修复。
- 该修复已通过静态回归测试和本地固件编译；仍必须按抬轮台架流程验证后再进行地面测试。

## 2026.06.11

### [FRC 双锚风险记忆工程入口补齐]

#### File / 文件

- `Makefile`
- `scripts/launch_with_logs.sh`
- `scripts/init_runtime_data.sh`
- `scripts/data_collection/record_bag.sh`
- `src/bringup/config/master_params.yaml`
- `src/bringup/config/nav2_explore.yaml`
- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_explore_gps.launch.py`
- `src/frc/frc_bringup/launch/frc_stack.launch.py`
- `docs-CN/knowledge/frc.md`
- `docs-EN/knowledge/frc.md`

#### Change / 改动

- 分层构建目标补入 `frc_msgs`，并统一加 `--parallel-workers 1`。
- `FRC_MODE=off|shadow|full` 接入 Explore 与 Explore-GPS；`FRC_EXTRA_PARAMS` 可追加 FRC 参数覆盖文件。
- `master_params.yaml` 新增 FRC 在线节点参数节与 PGO `"frc.*"` 导出参数。
- `init_runtime_data.sh` 创建 `runtime-data/frc/{models,routes,trials,events,anchor_log}`。
- `record_bag.sh` 新增 `PROFILE=frc`，追加 FRC、costmap、PGO keyframes、退化度量和 Nav2 状态 topic，优先写入 `$FYP_LOG_SESSION_DIR/rosbag2`，并启用 zstd file compression。
- `nav2_explore.yaml` local costmap 注册 `frc_layer`，默认 `enabled=false`。
- 新增中英文 FRC 知识页，记录运行模式、topic、参数覆盖、录包方式和 costmap 注入安全不变量。

#### Reason / 原因

- FRC 在线节点需要 `frc_msgs` 先构建，否则 `serial_reader` 与 `pgo` 的分层构建会缺接口依赖。
- `shadow` 与 `full` 必须使用相同在线节点集，唯一差异应是 `frc_layer.enabled`，这样 shadow bag 与闭环 bag 才能同构对比。
- `frc_layer` 是 tuned YAML 改动，默认旁路可保证基线行为不因装载插件而改变；闭环实验通过 `/frc/enable` 显式进入。
- FRC 数据闭环依赖统一 session 目录、FRC runtime 子目录和固定录包 profile。

#### Effect / 影响

- `make build-sensor`、`make build-perception`、`make build-frc` 的依赖关系更完整，并符合 Jetson 单 worker 构建约束。
- `FRC_MODE=shadow make launch-explore` 可启动 FRC 在线观测链而不注入 costmap。
- `FRC_MODE=full make launch-explore` 会延时调用 `/frc/enable` 打开风险注入。
- `PROFILE=frc bash scripts/data_collection/record_bag.sh` 可采集后续离线挖掘、BEV 构建和实验指标所需 topic，并与当前日志 session 对齐。
- 后续若调整 `frc_layer` 或 FRC 参数，必须同步更新本知识页与 devlog。
