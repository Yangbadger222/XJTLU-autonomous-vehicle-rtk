# June 2026 Development Log

---

## 2026.06.29

### [RTK basic launch file remapping]

#### File

- `scripts/launch_with_logs.sh`

#### Change

- The parameter `LAUNCH_FILE` for `rtk-basic` was changed to `LAUNCH_FILE="system_rtk_basic.launch.py"` (previous was `LAUNCH_FILE="um982_rtk.launch.py"`)
- The parameter `LAUNCH_PACKAGE` for `rtk-basic` was removed (previous was `LAUNCH_PACKAGE="um982_rtk_driver"`)

#### Reason

The script `launch_with_logs.sh` was set to directly use the launch file from the RTK driver instead of the general system launch files in the main `bringup` package. This change allows to remain consistent and use only `system_*.launch.py` files, which can be expanded to include other configuration and launch components.

#### Effect

Running `make launch rtk-basic` now calls `system_rtk_basic.launch.py`, which calls the original `um982_rtk.launch.py`. At the moment, this has no effect when running the robot. In terms of code, it allows to easily add other launch components to the `rtk-basic` mode, such as the URDF.

## 2026.06.25

### [Hardware Documentation Update & Foxglove Setup]

#### File

- `docs-EN/hardware_spec.md`
- `docs-CN/hardware_spec.md`
- `docs-EN/commands.md`
- `docs-CN/commands.md`
- `docs-EN/known_issues.md`
- `docs-CN/known_issues.md`
- `docs-EN/architecture.md`
- `docs-CN/architecture.md`

#### Change

- Updated previous documentation with recent hardware replacements, such as the Seeed Carrier board and RTK UM982
- Updated `Logs and Runtime Data` folder structure with new `bags/` folder for recent rosbags
- Added Foxglove setup instructions as a potential replacement for RViz
- Added an issue regarding some GPS parameters in `master_params.yaml`

#### Reason

- Fundamental hardware components were changed weeks ago, but the hardware documentation remained unchanged
- The `runtime_data/` folder has been updated and uploaded to Huggingface with new folders, which were not written in the `architecture.md` files
- RViz is inconvenient to use due to latency and requiring the Jetson to run it; Foxglove runs on the personal computer and supports virtually all features of RViz while allowing a more stable connection than ToDesk or AnyDesk
- Foxglove supports rosbag playback, facilitating data analysis with a better interface
- The GPS parameters in `master_params.yaml` were tuned for the previous GPS antenna, which has been discarded and replaced by the more precise RTK antenna

#### Effect

- `hardware_spec.md` now correctly reflects to-date information
- `architecture.md` correctly contains `bags/` as the folder where we recently upload rosbags
- Foxglove setup is documented and accessible for new teammates to easily analyze the robot behavior on real-time and from rosbags
- RViz settings in `src/bringup/rviz` must be carefully reviewed to ensure proper setup in Foxglove
- GPS parameters must be updated considering the new RTK hardware

## 2026.06.22

### [NTRIP Credentials Setup Automation]

#### File

- `docs-EN/commands.md`
- `docs-CN/commands.md`
- `scripts/setup_ntrip.py`
- `scripts/setup_ntrip.sh`

#### Change

- Added `scripts/setup_ntrip.sh` and `scripts/setup_ntrip.py` to automate the parsing of raw NTRIP account strings pasted directly from Taobao vendors.
- Configured the automation to generate a temporary configuration file `/tmp/um982_cors.yaml` and cache required environment variables (`FYP_RTK_PARAMS_FILE`, `NTRIP_PASSWORD`) inside `/tmp/ntrip_env.sh`.
- Updated both English and Chinese command reference documentation (`docs-EN/commands.md`, `docs-CN/commands.md`) to include execution details for the new NTRIP setup workflow.

#### Reason

- Raw NTRIP credentials provided by vendors usually arrive as single-line strings that are tedious and error-prone to manually format into a driver-compatible YAML format in the field.
- Avoids checking plaintext passwords or hardcoded caster configurations into the repository, ensuring credential security and codebase cleanliness.

#### Effect

- Streamlines field bring-up; developers can now configure the RTK CORS node with a single copy-paste operation.
- Eliminates the need to re-paste credentials when opening new terminal sessions; the active environment can be instantly recovered by sourcing `/tmp/ntrip_env.sh`.

## 2026.06.22

### [Huggingface]

#### File
- `docs-EN/commands.md`
- `docs-CN/commands.md`

#### Change

- Installed Huggingface CLI in the Jetson using:
```bash
pip install -U "huggingface_hub[cli]"
export HF_ENDPOINT=https://hf-mirror.com
hf auth login
```

- Created a Huggingface dataset in a shared organization:
```bash
hf repos create my-rtk-data --type dataset
```

- Added commands for uploading and cloning the rosbags from the Jetson to a personal computer

#### Reason

- Allows us to upload and track rosbags for further analysis into a shared repo

#### Effect

The path `./runtime-data/bags` is now for exclusive use of saving rosbags.

## 2026.06.18

### [Corridor RTK Heading Bootstrap Alignment]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_global_aligner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/alignment_math.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_alignment_math.py`
- `src/bringup/config/master_params.yaml`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`
- `docs-CN/known_issues.md`
- `docs-EN/known_issues.md`

#### Change

- Added a `/heading` subscription to `gps_global_aligner_node` and collect stable dual-antenna heading during startup.
- Changed bootstrap rotation to prefer live RTK heading: `theta = map_yaw - rtk_heading_enu_yaw`.
- Kept `launch_yaw_deg` as the fallback when heading is unavailable, and log a warning when it differs significantly from RTK heading.
- Added the pure `alignment_math.py` module covering heading-convention conversion, bootstrap yaw selection, and `theta/tx/ty` calculation.
- Added heading-bootstrap parameters to `master_params.yaml`: `use_rtk_heading_for_bootstrap`, `heading_quaternion_yaw_is_compass`, startup sample count, spread limit, and mismatch warning threshold.

#### Reason

- UM982 RTK `/fix` can provide high-precision ENU positions, but Nav2 goals must be projected into FAST-LIO's `map` frame.
- The old corridor bootstrap primarily depended on route-file `launch_yaw_deg`; a few degrees of initial yaw error can create meter-level lateral projection error at distant waypoints.
- The current UM982 `/heading` already represents calibrated vehicle heading, so it should participate in the initial ENU->map rotation instead of being only an observation topic.

#### Effect

- During outdoor corridor startup, the `ENU->map` rotation now prefers dual-antenna RTK heading, reducing goal projection error caused by stale `launch_yaw_deg` or manual heading confirmation.
- If stable `/heading` is unavailable in the field, the system falls back to the previous route `launch_yaw_deg` behavior instead of blocking startup.
- Runtime alignment still keeps translation-only smoothing to avoid rotating the active route during navigation; multi-point rigid-body registration remains the stronger future anchoring approach.

## 2026.06.17

### [UM982 RTK C++ Basic Chain]

#### File

- `src/sensor_drivers/gnss/um982_rtk_driver/`
- `src/bringup/launch/system_rtk_basic.launch.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/launch/system_nav_gps.launch.py`
- `src/sensor_drivers/gnss/gnss_calibration/launch/gnss_calibration_launch.py`
- `src/bringup/config/master_params.yaml`
- `src/sensor_drivers/gnss/nmea_navsat_driver/config/nmea_serial_driver.yaml`
- `src/bringup/package.xml`
- `Makefile`
- `scripts/launch_with_logs.sh`
- `docs-CN/handoff/rtk_um982_basic_chain.md`
- `docs-EN/handoff/rtk_um982_basic_chain.md`

#### Change

- Added the C++ `um982_rtk_driver` package to own `/dev/rtk_um982` directly and parse GGA/RMC/THS/HPR/UNIHEADINGA.
- Published `/fix`, `/heading`, `rtk/nmea_sentence`, and `rtk/status`, preserving downstream `/fix` consumption.
- Added an optional C++ NTRIP client, disabled by default; CORS credentials are read only from parameters or environment variables and are not stored in the repository.
- Added `make launch-rtk-basic` / `system_rtk_basic.launch.py` for first-stage field smoke testing.
- Added `make build-rtk-basic` and made the `rtk-basic` log mode launch `um982_rtk_driver` directly, so light smoke tests are not blocked by `bringup` full-vehicle runtime dependencies.
- Added `FYP_RTK_PARAMS_FILE` as a field parameter entrypoint that passes temporary CORS/NTRIP YAML only to the UM982 driver.
- Switched `explore-gps`, `nav-gps`, `corridor`, and `gnss_calibration` GNSS startup paths to the C++ UM982 driver.
- Added `um982_rtk_node` and `/dev/rtk_um982` to runtime cleanup.
- Added UM982 parser gtests for GGA quality 4, THS, HPR, UNIHEADINGA, and bad checksums.
- Added the `heading_offset_deg` parameter, currently set to `90.0`, to convert the right-master / left-secondary lateral dual-antenna heading into the `base_link +X` vehicle heading; `rtk/status` now shows both calibrated and raw heading.

#### Reason

- Field bring-up should prefer C++ for the RTK basic chain, reduce resident Python processes, and avoid separate monitor/NTRIP processes competing for the same serial port.
- UM982 dual-antenna heading may arrive through THS/HPR or Unicore `UNIHEADINGA`, so the basic chain needs to support both paths.
- Field observation showed `/heading` near east while the vehicle front was near south, matching the current lateral-baseline installation being about 90 degrees offset from vehicle forward.
- RTK Fixed/Float cannot be determined from `NavSatStatus` alone; raw GGA/status output must stay available for field acceptance.

#### Effect

- Field testing can start with `make launch-rtk-basic` to verify `/fix`, `/heading`, `rtk/status`, and raw NMEA before enabling navigation modes.
- A fresh-cloned Jetson can run `make build-rtk-basic` first for RTK smoke testing; full GPS navigation modes still require the normal full-stack vehicle build.
- Field testing can set `FYP_RTK_PARAMS_FILE=/tmp/um982_cors.yaml` to reuse the same untracked CORS parameter file across basic smoke tests and GPS navigation modes.
- `/heading` now represents the calibrated vehicle heading; if antenna master/secondary wiring or front-back mounting changes later, adjust only `heading_offset_deg`.
- GPS-related navigation modes now default to the UM982 at `/dev/rtk_um982`; the old G60 path remains only as a fallback, not the primary entrypoint.
- The current NTRIP implementation supports plain TCP casters. If the CORS caster requires TLS, add TLS support later or let the receiver/vendor 4G module own NTRIP.

### [Chassis Angular-Velocity Mapping And Serial Export]

#### File

- `src/bringup/config/master_params.yaml`
- `src/sensor_drivers/serial/CMakeLists.txt`
- `src/sensor_drivers/serial_twistctl/`

#### Change

- Added the `angular_z_scale` parameter to `serial_twistctl_node`, making the ROS `angular.z` to lower-controller `wc` sign/scale mapping explicit. Field testing currently confirms `1.0`: positive turns left and negative turns right.
- Extracted serial command formatting into `serial_twistctl/twist_command.hpp` and added a gtest for both default and inverted mappings.
- Fixed the `serial` package CMake install/export so `serial_twistctl_node` links against the installed `libserial.so` on the Jetson.

#### Reason

- Field navigation showed right drift / left half-circle behavior, so the ROS angular-velocity to STM32 `wc` sign relationship needed to be verifiable and configurable instead of hard-coded.
- Jetson package-level builds had failed to link `serial_twistctl` because the `serial` library did not export an install-space target correctly.

#### Effect

- Field tests can verify steering sign with short angular-velocity pulses; if firmware or wiring changes later, switch only `angular_z_scale` to `-1.0`.
- `serial_twistctl` package builds and tests no longer depend on a build-tree-only serial library path.

### [Corridor RTK Diagnostic Bagging]

#### File

- `src/bringup/launch/system_gps_corridor.launch.py`

#### Change

- Added `/heading`, `/rtk/status`, and `/rtk/nmea_sentence` to the automatic corridor-mode rosbag topic list.

#### Reason

- GPS corridor field debugging needs RTK quality, raw NMEA, and dual-antenna heading in the same bag; otherwise we can only infer RTK state from `/fix`, TF, and Nav2 status.

#### Effect

- The next `scripts/launch_with_logs.sh corridor` bag can directly correlate RTK Fixed/Float state, heading offset, raw GGA/UNIHEADING data, and Nav2 recovery behavior.

## 2026.06.14

### [Lower-Controller B Button Damped Active Braking]

#### File

- `src/firmware/rm_c_board/Core/Src/Joystick.c`
- `src/firmware/rm_c_board/Core/Src/Motor_Speed_pid.c`
- `src/firmware/rm_c_board/Core/Inc/Motor_Speed_pid.h`
- `src/firmware/rm_c_board/Core/Src/main.c`
- `src/firmware/rm_c_board/test/test_estop_safety_contract.py`
- `src/firmware/rm_c_board/README.md`
- `src/frc/frc_msgs/msg/ChassisStatus.msg`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/hardware_spec.md`
- `docs-EN/hardware_spec.md`
- `docs-CN/known_issues.md`
- `docs-EN/known_issues.md`
- `docs-CN/knowledge/frc.md`
- `docs-EN/knowledge/frc.md`

#### Change

- Added `brake_flag` and `Active_Brake_Output()`, so pressing `B` no longer follows the `motor_shutdown/free_flag` coast-disable path.
- `B` now locks `Vcx/Wc=0` and runs a dedicated damped-braking output from `Speed_set()` at 200 Hz instead of reusing the drive PID output.
- Damped braking generates current from `-BRAKE_DAMPING_KP * speed_rpm`, preserves `BRAKE_CURRENT_LIMIT=8000` at higher speed, tapers only below `BRAKE_LOW_SPEED_RPM=300` with `BRAKE_LOW_SPEED_CURRENT_LIMIT=4500`, and limits each-cycle current changes with `BRAKE_CURRENT_STEP=1200`; `BRAKE_DAMPING_KP=12.0` reaches the strong-brake limit sooner at medium and high speed.
- The `B` brake latch now initializes only on the first `brake_flag == 0` trigger frame, so holding `B` no longer repeatedly calls `Clear_Brake_State()` and resets the current ramp; the `B` indicator no longer calls blocking `led_pink_blink()` and now uses non-blocking solid pink.
- After all wheels enter the `BRAKE_STOP_RPM=120` threshold, the firmware clears brake and PID state and returns to zero-current output to avoid pulling the wheels through zero speed.
- `X`, KEY, and gamepad-loss paths still use zero-current coast-stop semantics; the upstream `ctrl_mode` reports a safety takeover while `B` active braking is latched.
- Updated the static regression test and bilingual safety documentation.

#### Reason

- Vehicle-side operation needs `B` to actively stop the chassis instead of only disabling motor output.
- The old `B` PID-brake path previously caused reversal because integral limiting was ineffective and reverse current continued after zero-speed crossing; field testing then showed that the ordinary limited-PID brake still produced noticeable fore-aft oscillation, so `B` now uses a dedicated curve with strong high-speed braking and damped low-speed finishing.
- On-vehicle testing still showed a short coast-before-brake delay; one root cause was repeated ramp-state clearing while `B` was held, plus the old pink blink function blocking scheduling through `HAL_Delay()`.

#### Effect

- `B` now differs from `X`: `X` is coast-disable, while `B` is damped active braking followed by zero-current release near stop.
- Holding `B` should now enter the continuous braking phase sooner, avoiding front-end braking latency from repeated ramp resets or LED delay.
- This behavior passed the static regression test and local STM32 GCC compilation; wheel-off bench validation is still required before ground testing.

## 2026.06.13

### [FRC Online Node C++ Migration]

#### File

- `src/frc/frc_nodes_cpp/package.xml`
- `src/frc/frc_nodes_cpp/CMakeLists.txt`
- `src/frc/frc_nodes_cpp/include/frc_nodes_cpp/health_aggregator_core.hpp`
- `src/frc/frc_nodes_cpp/include/frc_nodes_cpp/event_marker_core.hpp`
- `src/frc/frc_nodes_cpp/include/frc_nodes_cpp/risk_pipeline_core.hpp`
- `src/frc/frc_nodes_cpp/src/health_aggregator_node.cpp`
- `src/frc/frc_nodes_cpp/src/event_marker_node.cpp`
- `src/frc/frc_nodes_cpp/src/risk_pipeline_node.cpp`
- `src/frc/frc_nodes_cpp/test/test_health_aggregator_core.py`
- `src/frc/frc_nodes_cpp/test/test_event_marker_core.py`
- `src/frc/frc_nodes_cpp/test/test_risk_pipeline_core.py`
- `src/frc/frc_bringup/launch/frc_stack.launch.py`
- `src/frc/frc_bringup/package.xml`
- `Makefile`
- `docs-CN/knowledge/frc.md`
- `docs-EN/knowledge/frc.md`

#### Change

- Added the C++ `frc_nodes_cpp` package with `frc_health_aggregator`, `frc_event_marker`, and `frc_risk_pipeline` executables.
- Moved health aggregation rules for stale inputs, LIO degeneracy, PGO correction status, RTK status, and chassis velocity into a ROS-free C++ core header.
- Moved event-marker rules for takeover edges, SELECT manual markers, stuck, near-collision, recovery, jerk, and plan instability into C++, while preserving JSONL mirror output.
- Moved the default `frc_risk_pipeline` online path to C++: map-anchor Gaussian risk rendering, `/frc/risk_grid` publishing, overrun degradation, and debug BEV output.
- Switched `frc_health_aggregator`, `frc_event_marker`, and `frc_risk_pipeline` in `frc_stack.launch.py` to the C++ package; `frc_memory_manager` still runs from Python `frc_nodes`.
- Added `frc_nodes_cpp` to `make build-frc` and to `frc_bringup` runtime dependencies.
- Added a local `g++`/pytest core contract test.

#### Reason

- `frc_health_aggregator`, `frc_event_marker`, and `frc_risk_pipeline` are long-running Python ROS nodes in FRC shadow/full mode; moving them reduces resident Python interpreter processes.
- Migrating a low-risk node first validates package layout, build wiring, and launch switching while keeping the Python implementation available as a rollback reference.

#### Effect

- FRC shadow/full runs now have three fewer Python resident processes, which should reduce Jetson memory use.
- `/frc/health`, `/frc/event_marker`, `/frc/risk_grid`, node names, and parameters are unchanged, so downstream `frc_costmap_layer` and `frc_memory_manager` do not need changes.
- The C++ `frc_risk_pipeline` does not yet load prototype NPZ files or TensorRT engines; if those files are configured it warns and continues with map-anchor risk.
- This workstation has no ROS Humble environment, so verification covered the ROS-free core test, launch Python syntax, XML parsing, `git diff --check`, and `make -n build-frc`; the Jetson still needs `colcon build --packages-select frc_msgs frc_nodes_cpp frc_bringup --symlink-install --parallel-workers 1`.

### [Lower-Controller Stop-Path Wheel-Reversal Fix]

#### File

- `src/firmware/rm_c_board/Core/Src/main.c`
- `src/firmware/rm_c_board/Core/Src/Motor_Speed_pid.c`
- `src/firmware/rm_c_board/Core/Inc/Motor_Speed_pid.h`
- `src/firmware/rm_c_board/Core/Src/Joystick.c`
- `src/firmware/rm_c_board/Core/Src/pid.c`
- `src/firmware/rm_c_board/Makefile`
- `src/firmware/rm_c_board/test/test_estop_safety_contract.py`
- `src/firmware/rm_c_board/README.md`
- `docs-CN/hardware_spec.md`
- `docs-EN/hardware_spec.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/known_issues.md`
- `docs-EN/known_issues.md`

#### Change

- Changed the KEY stop branch from task shutdown plus blocking LED blink to sustained `Emergency_Stop_Output()` zero-current CAN output with a non-blocking solid red LED.
- Replaced invalid `Set_free;` / `led_white_start;` expressions with real function calls, and added `Emergency_Stop_Output()` to clear `Vcx/Wc`, target wheel speeds, PID outputs, and PID targets.
- Changed PS2 `B` from PID zero-speed braking to the same coast-stop disable path as `X`, with immediate zero-current output.
- Restored PID integral limiting and `last_err` updates, clamped PID step time to 1-50 ms, and added `-Werror=unused-value` to the firmware Makefile.
- Added a ROS-free static regression test for the KEY/B/X stop paths and PID safety contract.
- Updated safety docs to state that `X` remains the highest-priority software disable, the red physical e-stop overrides all software commands, and `B` must not replace those stops before bench and vehicle validation.

#### Reason

- The old KEY path set `stop_flag` and stopped task scheduling, so chassis CAN commands were no longer refreshed and the C620 controllers could latch the last current frame.
- The old `Set_free;` statements only evaluated a function name expression; they did not send zero current or clear PID state.
- The old `B` path used PID zero-speed braking, and disabled integral limiting could keep producing reverse current after wheel speed crossed zero.

#### Effect

- KEY, `X`, `B`, and gamepad-loss stop paths should now enter zero-current coast stop instead of stopping CAN output or relying on PID braking.
- Normal serial/gamepad driving PID gains are unchanged, but integral limiting is now active; if bench testing shows tracking changes, evaluate `IntegralLimit` separately instead of reverting the stop-path safety fix.
- The fix passed static regression tests and local firmware compilation; wheel-off bench validation is still required before ground testing.

## 2026.06.11

### [FRC Dual-Anchor Risk Memory Engineering Entrypoints]

#### File

- `Makefile`
- `scripts/launch_with_logs.sh`
- `scripts/init_runtime_data.sh`
- `scripts/data_collection/record_bag.sh`
- `src/bringup/config/master_params.yaml`
- `src/bringup/config/nav2_explore.yaml`
- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_explore_gps.launch.py`
- `src/frc/frc_bringup/launch/frc_stack.launch.py`
- `docs-CN/knowledge/frc.md`
- `docs-EN/knowledge/frc.md`

#### Change

- Added `frc_msgs` to layered build targets and made the targets use `--parallel-workers 1`.
- Wired `FRC_MODE=off|shadow|full` into Explore and Explore-GPS; added `FRC_EXTRA_PARAMS` for appended FRC parameter overrides.
- Added FRC online-node parameter sections and PGO `"frc.*"` export parameters to `master_params.yaml`.
- Made `init_runtime_data.sh` create `runtime-data/frc/{models,routes,trials,events,anchor_log}`.
- Added `PROFILE=frc` to `record_bag.sh`, appending FRC, costmap, PGO keyframe, degeneracy, and Nav2 status topics, defaulting output to `$FYP_LOG_SESSION_DIR/rosbag2`, and using zstd file compression.
- Registered `frc_layer` in the `nav2_explore.yaml` local costmap with `enabled=false` by default.
- Added bilingual FRC knowledge docs covering runtime modes, topics, parameter overrides, bag recording, and costmap injection safety invariants.

#### Reason

- FRC online nodes require `frc_msgs` to be built first; otherwise layered `serial_reader` and `pgo` builds miss their new interface dependency.
- `shadow` and `full` must run the same online node set, with `frc_layer.enabled` as the only behavior difference, so shadow and closed-loop bags remain structurally identical.
- `frc_layer` is a tuned YAML change. Keeping it disabled by default preserves baseline behavior until `/frc/enable` explicitly turns on injection.
- The FRC data loop needs a shared session layout, runtime subdirectories, and a fixed recording profile.

#### Effect

- `make build-sensor`, `make build-perception`, and `make build-frc` now have complete dependency entrypoints and follow the Jetson single-worker build constraint.
- `FRC_MODE=shadow make launch-explore` can start the FRC observation path without costmap injection.
- `FRC_MODE=full make launch-explore` delays and then calls `/frc/enable` to enable risk injection.
- `PROFILE=frc bash scripts/data_collection/record_bag.sh` records the topics required for offline mining, BEV generation, and trial metrics while staying aligned with the current log session.
- Future FRC parameter or `frc_layer` tuning changes must update the knowledge page and devlog together.
