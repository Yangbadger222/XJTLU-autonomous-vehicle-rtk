<!--
  Travel prior-map Nav2 Behavior Tree.

  This tree intentionally omits motion recovery actions. In tight indoor tests,
  controller failures must stop and fail the goal instead of triggering automatic
  Spin or BackUp maneuvers near desks, chairs, or walls.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <PipelineSequence name="NavigateWithFailStop">
      <RateController hz="2.0">
        <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
      </RateController>
      <FollowPath path="{path}" controller_id="FollowPath"/>
    </PipelineSequence>
  </BehaviorTree>
</root>
