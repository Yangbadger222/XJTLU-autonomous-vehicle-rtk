collision_monitor:
  ros__parameters:
    use_sim_time: false
    base_frame_id: base_footprint
    odom_frame_id: odom
    cmd_vel_in_topic: /cmd_vel_localized
    cmd_vel_out_topic: /cmd_vel_safe
    transform_tolerance: 0.3
    source_timeout: 0.5
    base_shift_correction: true
    stop_pub_timeout: 2.0
    polygons: ["PolygonStop", "PolygonSlow"]
    PolygonStop:
      type: polygon
      # Starts outside the 0.40 x 0.30m self-filter and retains a real stop band.
      points: [0.45, 0.35, 0.45, -0.35, -0.45, -0.35, -0.45, 0.35]
      action_type: stop
      max_points: 3
      visualize: true
      polygon_pub_topic: /travel_safety/stop_zone
      enabled: true
    PolygonSlow:
      type: polygon
      # Forward-only buffer outside the 0.35m front footprint. Travel MPPI does
      # not reverse, so rear/side returns must not permanently throttle forward motion.
      points: [0.85, 0.30, 0.85, -0.30, 0.45, -0.30, 0.45, 0.30]
      action_type: slowdown
      max_points: 3
      slowdown_ratio: 0.60
      visualize: true
      polygon_pub_topic: /travel_safety/slow_zone
      enabled: true
    observation_sources: ["pointcloud"]
    pointcloud:
      type: pointcloud
      topic: /fastlio2/body_cloud_nav2
      min_height: -0.20
      max_height: 1.20
      enabled: true
